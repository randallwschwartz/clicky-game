import React from "react";

const Header = () => (
  <div className="jumbotron p-3">
    <h1 className="text-center">Car Clicky Game!</h1>
    <h3 className="text-center">Click on each car only once to earn points.  Clicking a car more than once resets the game.</h3>
  </div>

)
export default Header;